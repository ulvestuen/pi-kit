{
  "name": "pi-orchestrator",
  "version": "1.0.0",
  "description": "Thin multi-agent composition layer for the pi coding agent — /orchestrate plans a goal as a task DAG, dispatches waves of fleet sub-agents, gates tasks behind independent critic review, and drives the run inside a lykkja PDCA loop",
  "type": "module",
  "scripts": {
    "test": "tsx --test test.ts"
  },
  "pi": {
    "extensions": ["./index.ts"],
    "skills": ["./skills"]
  },
  "devDependencies": {
    "@pi-kit/agent-types": "workspace:*",
    "@mariozechner/pi-ai": "^0.71.0",
    "@mariozechner/pi-coding-agent": "^0.71.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
