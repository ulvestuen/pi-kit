/** Compatibility re-exports; shared registry contracts live in agent-types. */
export {
  THINKING_LEVELS,
  parseAgentDefinition,
  mergeRegistries,
  getAgent,
  type ThinkingLevel,
  type AgentDefinition,
  type DiscoveredRegistry,
} from "@pi-kit/agent-types";
