{
  "name": "pi-critic",
  "version": "1.0.0",
  "description": "Independent advisor/reviewer for the pi coding agent — critic_review scores work against explicit criteria with a fresh-context read-only sub-agent, critic_advise gives pre-implementation design feedback",
  "type": "module",
  "scripts": {
    "test": "tsx --test test.ts"
  },
  "pi": {
    "extensions": ["./index.ts"],
    "skills": ["./skills"]
  },
  "devDependencies": {
    "@mariozechner/pi-ai": "^0.71.0",
    "@mariozechner/pi-coding-agent": "^0.71.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
